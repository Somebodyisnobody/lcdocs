name: Build

on:
  push:
    paths:
      - 'sdk/**/*.xml'
      - clonk.xsl
      - '**/*.css'
      - '**/*.js'
      - '**/*.py'
      - '**/*.pl'
      - 'Makefile'
      - 'images/**'
      - '*.po'
  pull_request:
    types: [ ready_for_review ]

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build docs
    # 2022-04-12 libxml2 is not available for ubuntu 22 which is the ubuntu-latest option: https://packages.ubuntu.com/search?keywords=python-libxml2
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3.9.0
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Setup saxonb-xslt, python, gettext
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install --no-install-recommends libsaxonb-java python-libxml2 gettext node-typescript -y

      - name: Build docs
        run: |
          make -j$(nproc)

      - name: "Collect artifacts"
        run: |
          mkdir output
          mv online output
          mv chm output
          mv sdk-en output
          mv sdk output
          mv doku.pot output
          mv en.mo output
          mv Functions.txt output
          cd output
          zip -q -0 -r ../generated-docs.zip *

      - name: "Publish zipped Main Artifact"
        uses: actions/upload-artifact@v3
        with:
          name: generated-docs-nested-zip
          path: generated-docs.zip
