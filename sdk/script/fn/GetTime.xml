<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<?xml-stylesheet type="text/xsl" href="../../../clonk.xsl"?>
<clonkDoc xmlns="https://clonkspot.org"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="https://clonkspot.org ../../../clonk.xsd">
	<func>
		<title>GetTime</title>
		<category>System</category>
		<versions><version>4.9.1.0 GWE</version></versions>
		<syntax>
			<rtype>int</rtype>
		</syntax>
		<desc>Gibt die Zeit seit Windowsstart in Millisekunden zurück. Kann zur genauen Zeitmessung
					verwendet werden.</desc>
		<remark>Da die Zeiten bei unterschiedlichen Computern variieren können, gibt diese Funktion,
						um eine Syncunsicherheit zu vermeiden, beim Netzwerkspiel und bei Filmaufnahmen 0
						zurück.</remark>
		<examples>
			<example>
				<code>func TestCos()
{
	if(<funclink>IsNetwork</funclink>()) <funclink>return</funclink>(<funclink>Log</funclink>("TestCos funktioniert im Netzwerk nicht!"));
	var iStartTime = GetTime();
	for(var i = 0; i &lt; 100; i++) var x = <funclink>Cos</funclink>(i, 100);
	var iTime = GetTime() - iStartTime;
	<funclink>Log</funclink>("Ein Cos-Aufruf benötigt: %d.%02d ms", iTime / 100, iTime % 100);
}</code>
				<text>Diese Funktion prüft die Performance der Funktion "<funclink>Cos</funclink>".
							Dabei wird die Funktion 100-mal aufgerufen und die verbrauchte Zeit mittels GetTime()
							gemessen.</text>
			</example>
		</examples>
		<related>
			<funclink>GetSystemTime</funclink>
		</related>
	</func>
	<author>Peter</author><date>2002-08</date>
</clonkDoc>
