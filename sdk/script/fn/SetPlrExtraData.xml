<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<?xml-stylesheet type="text/xsl" href="../../../clonk.xsl"?>
<clonkDoc xmlns="https://clonkspot.org"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="https://clonkspot.org ../../../clonk.xsd" xml:lang="de">
 <func>
  <title>SetPlrExtraData</title>
  <category>Spieler</category>
  <versions><version>4.9.1.0 GWE</version></versions>
  <syntax>
   <rtype>any</rtype>
   <params>
    <param>
     <type>int</type>
     <name>iPlr</name>
     <desc>Nummer des Spielers, für den zusätzliche Daten gespeichert werden sollen</desc>
    </param>
    <param>
     <type>string</type>
     <name>strDataName</name>
     <desc>Name, der den Daten zugeordnet werden soll</desc>
    </param>
    <param>
     <type>any</type>
     <name>Data</name>
     <desc>Daten, die gesetzt werden sollen.</desc>
    </param>
   </params>
  </syntax>

  <desc>Mithilfe dieses Befehls können beliebige, scriptdefinierte Werte unter einem gegebenen Namen in der Spielerdatei gespeichert werden.
        Auf diese Weise kann man Daten über einen Spieler über Szenariogrenzen hinweg erhalten (z.B. für RPGs)
        Zurückgegeben wird (wenn erfolgreich) der gesetzte Wert.</desc>

  <remark>Es dürfen keine Objektpointer oder Zeichenfolgen (Strings) gespeichert werden.</remark>

  <remark>Da die Namen des ExtraData über Szenariogrenzen hinweg gültig sind, sollte man dem Namen einen Prefix hinzufügen, um Doppelbenutzungen zu vermeiden ("MySzen_MyName").</remark>

  <examples>
   <example>
    <code>Timer:
  for(var i=0; i&lt;<funclink>GetPlayerCount</funclink>(); i++)
    SetPlrExtraData(i, "MySzen_Wealth", <funclink>GetWealth</funclink>(i));
  return();

Initialize:
  for(var i=0; i&lt;<funclink>GetPlayerCount</funclink>(); i++)
    <funclink>SetWealth</funclink>(i, <funclink>GetPlrExtraData</funclink>(i, "MySzen_Wealth"));
  return();
  </code>
    <text>Die erste Funktion speichert periodisch das Vermögen unter dem Namen "MySzen_Wealth" in dem entspr. Spieler ab. Wird die Runde nun neu gestartet und das Objekt
          erstellt, so wird das Vermögen der Spieler in Initialize wiederhergestellt.</text>
   </example>
  </examples>
  <related>
   <funclink>GetPlrExtraData</funclink>
   <funclink>SetCrewExtraData</funclink>
  </related>
 </func>
 <author>PeterW</author><date>2002-01</date>
</clonkDoc>
