<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<?xml-stylesheet type="text/xsl" href="../../../clonk.xsl"?>
<clonkDoc xmlns="https://clonkspot.org"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="https://clonkspot.org ../../../clonk.xsd" xml:lang="de">
  <func isAsync="false">
    <title>GetDefCoreVal</title>
    <category>Get*Val</category>
    <versions><version>4.9.1.0 GWE</version></versions>
    <syntax>
      <rtype isReference="false">any</rtype>
      <params>
        <param isReference="false" isOptional="false">
          <type>string</type>
          <name>strEntry</name>
          <desc>Name des abzufragenden Eintrags</desc>
        </param>
        <param isReference="false" isOptional="true">
          <type>string</type>
          <name>strSection</name>
          <desc>Sektion des abzufragenden Eintrags. Wird 0 übergeben, werden alle Sektionen nach einem Eintrag mit dem entspr. Namen durchsucht.</desc>
        </param>
        <param isReference="false" isOptional="true">
          <type>id</type>
          <name>idDef</name>
          <desc>Objektdefinition, von der der Eintrag abgefragt werden soll. Bei Übergabe von 0 wird die Definition des aufrufenden Objekts angenommen.</desc>
        </param>
        <param isReference="false" isOptional="true">
          <type>int</type>
          <name>iEntryNr</name>
          <desc>Nummer des abzufragenden Wertes, wenn der Eintrag mehrere Werte enthält.</desc>
        </param>
      </params>
    </syntax>

    <desc>Gibt einen Eigenschaftswert einer Objektdefinition nach seinem Namen in der <emLink href="definition/defcore.html">DefCore.txt</emLink>
          zurück.</desc>

    <remark>Ist der abzufragende Eintrag eine ID-Liste (z.B. "Components" in der DefCore.txt), so wird bei
            geradem iEntryNr die ID, bei ungeradem iEntryNr die Anzahl zurückgegeben (siehe Beispiel)</remark>

    <examples>
      <example>
        <code>var wdt = GetDefCoreVal("Width", "DefCore", GetID());</code>
        <text>Ermittelt die Breite des Objekts. Die beiden letzten Parameter könnten in diesem Fall weggelassen werden.</text>
      </example>
      <example>
        <code><funcLink>Log</funcLink>(" Eine Werkstatt benötigt folgendes Material:");

for(var i = 0;; i++)
{
  var id = GetDefCoreVal("Components", "DefCore", WRKS, i*2);
  if(!id) break;
  var cnt = GetDefCoreVal("Components", "DefCore", WRKS, i*2+1);

  <funcLink>Log</funcLink>("%d x %s", cnt, <funcLink>GetName</funcLink>(0, id));
}</code>
        <text>Fragt den Materialbedarf für den Bau einer Werkstatt ab. <br/> Die Schleife läuft solange, bis GetDefCoreVal für
              die ID 0 zurückgibt, was das Ende der Liste markiert</text>
      </example>
    </examples>


    <related>
      <funcLink>GetObjectVal</funcLink>
      <funcLink>GetObjectInfoCoreVal</funcLink>
      <funcLink>GetActMapVal</funcLink>
      <funcLink>GetScenarioVal</funcLink>
      <funcLink>GetPlayerVal</funcLink>
      <funcLink>GetPlayerInfoCoreVal</funcLink>
    </related>

  </func>
  <author>PeterW</author><date>2002-04</date>
</clonkDoc>
